% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/append.R
\name{append_data}
\alias{append_data}
\title{Append a dataset to an output collection}
\usage{
append_data(
  output_object,
  object_name = NULL,
  output_list = NULL,
  title = NULL,
  time_stamp = NULL,
  desc = NULL,
  vars = NULL,
  source = NULL,
  notes = NULL,
  format = NULL,
  subfolder = NULL,
  codebook = NULL,
  labels = NULL,
  replace = TRUE,
  quiet = NULL
)
}
\arguments{
\item{output_object}{A data frame, or any object coercible with
\code{\link[=as.data.frame]{as.data.frame()}}.}

\item{object_name}{Name to store and export the plot under. Defaults to
the name of \code{output_object}.}

\item{output_list}{The collection to append to. Defaults to \code{list_outputs}
if visible, otherwise the collection created by the last
\code{\link[=init_outputs]{init_outputs()}}. May be an object, or the name of one.}

\item{title}{Documentation: a human-readable title. Defaults to
\code{object_name}.}

\item{time_stamp}{Documentation: when the output was produced. Defaults to
the current time.}

\item{desc}{Documentation: a free-text description.}

\item{vars}{Documentation: the variables shown, ideally named, e.g.
\code{c(x = "Speed", y = "Braking distance")}.}

\item{source}{Documentation: data sources, ideally named, e.g.
\code{c("CZSO" = "Census 2021")}.}

\item{notes}{Documentation: caveats, to-dos, anything else worth recording.}

\item{format}{File format(s): any of \code{.csv}, \code{.csv2}, \code{.tsv}, \code{.xlsx},
\code{.rds}, \code{.parquet}, \code{.json}. Defaults to \code{gpars$formats$data}.}

\item{subfolder}{Subfolder within the plots folder. May be nested
(\code{"maps/regional"}).}

\item{codebook}{Write a codebook for this dataset. Defaults to
\code{gpars$codebook}.}

\item{labels}{Optional named character vector of variable labels, e.g.
\code{c(speed = "Speed in mph")}. Used in the codebook. Variable labels
already stored in a column's \code{label} attribute (as \code{haven} does) are
picked up automatically.}

\item{replace}{Overwrite an existing entry of the same name. Default \code{TRUE}.}

\item{quiet}{Suppress the confirmation message.}
}
\value{
The collection, invisibly.
}
\description{
Marks a dataset for export. Unlike \code{\link[=append_table]{append_table()}}, appended \emph{data} also
gets a variable-level codebook in the export (see \code{\link[=dr_codebook]{dr_codebook()}}); the
distinction is intent — \code{data} is the analysis-ready dataset you want
others to reuse, \code{tables} are presentation tables.
}
\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
data_cars <- cars
append_data(data_cars, output_list = out, format = c(".csv", ".rds"),
            labels = c(speed = "Speed (mph)", dist = "Distance (ft)"),
            quiet = TRUE)
names(out$data)
}
\seealso{
\code{\link[=append_table]{append_table()}}, \code{\link[=dr_codebook]{dr_codebook()}}, \code{\link[=save_outputs]{save_outputs()}}
}
