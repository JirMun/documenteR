% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/append.R
\name{append_plot}
\alias{append_plot}
\title{Append a plot to an output collection}
\usage{
append_plot(
  output_object,
  object_name = NULL,
  output_list = NULL,
  title = NULL,
  time_stamp = NULL,
  desc = NULL,
  vars = NULL,
  source = NULL,
  notes = NULL,
  device = NULL,
  dpi = NULL,
  type = NULL,
  H = NULL,
  W = NULL,
  units = NULL,
  bg = NULL,
  subfolder = NULL,
  wrap_labels = FALSE,
  replace = TRUE,
  quiet = NULL
)
}
\arguments{
\item{output_object}{The plot. Must be a named object, or pass
\code{object_name}.}

\item{object_name}{Name to store and export the plot under. Defaults to
the name of \code{output_object}.}

\item{output_list}{The collection to append to. Defaults to \code{list_outputs}
if visible, otherwise the collection created by the last
\code{\link[=init_outputs]{init_outputs()}}. May be an object, or the name of one.}

\item{title}{Documentation: a human-readable title. Defaults to
\code{object_name}.}

\item{time_stamp}{Documentation: when the output was produced. Defaults to
the current time.}

\item{desc}{Documentation: a free-text description.}

\item{vars}{Documentation: the variables shown, ideally named, e.g.
\code{c(x = "Speed", y = "Braking distance")}.}

\item{source}{Documentation: data sources, ideally named, e.g.
\code{c("CZSO" = "Census 2021")}.}

\item{notes}{Documentation: caveats, to-dos, anything else worth recording.}

\item{device}{File format(s) to export to, e.g. \code{".png"} or
\code{c(".png", ".svg")}. Defaults to \code{gpars$formats$plots}.}

\item{dpi}{Resolution for raster formats: a number, or one of
\code{"retina"} (320), \code{"print"} (300), \code{"screen"} (72).}

\item{type}{Name of a size preset in \code{gpars$sizes$type} (\code{"s"}, \code{"m_wide"},
...). Sets \code{H}, \code{W} and \code{units} unless you give them explicitly.}

\item{H, W}{Height and width. Override \code{type}.}

\item{units}{Units for \code{H} and \code{W}: \code{"cm"} (default), \code{"mm"}, \code{"in"}, \code{"px"}.}

\item{bg}{Background colour, e.g. \code{"white"} or \code{"transparent"}.}

\item{subfolder}{Subfolder within the plots folder. May be nested
(\code{"maps/regional"}).}

\item{wrap_labels}{If \code{TRUE}, wrap the plot's title, subtitle and axis
labels to the character widths in \code{gpars$textwidths} before export.
Only affects \code{ggplot2} objects. Default \code{FALSE}.}

\item{replace}{Overwrite an existing entry of the same name. Default \code{TRUE}.}

\item{quiet}{Suppress the confirmation message.}
}
\value{
The collection, invisibly. Called for its side effect.
}
\description{
Marks a plot for export and records its documentation and export geometry.
Any plotting system is accepted, not just \code{ggplot2} — see
\emph{Supported plot objects}.
}
\section{Supported plot objects}{

\itemize{
\item \code{ggplot2} objects, including \code{patchwork} and \code{ggarrange} compositions.
\item \code{lattice} / \code{trellis} objects.
\item \code{grid} grobs, \code{gtable}s and anything with a \code{grid.draw()} method.
\item A \strong{function of no arguments} that draws with base graphics, e.g.
\code{function() { plot(x, y); abline(h = 0) }}. This is the way to export
base-R plots, which cannot be captured as objects.
\item A recorded base plot from \code{grDevices::recordPlot()}.
\item Anything else with a \code{print()} or \code{plot()} method, as a last resort.
}

See \code{\link[=dr_draw]{dr_draw()}} to add support for another plotting system.
}

\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)

# A base-R plot, passed as a drawing function
plt_cars <- function() plot(cars$speed, cars$dist, pch = 19)
append_plot(plt_cars, output_list = out, title = "Braking distance",
            vars = c(x = "Speed (mph)", y = "Distance (ft)"),
            type = "m_wide", quiet = TRUE)

names(out$plots)
}
\seealso{
\code{\link[=append_data]{append_data()}}, \code{\link[=append_table]{append_table()}}, \code{\link[=append_stats]{append_stats()}},
\code{\link[=preview_plot]{preview_plot()}}, \code{\link[=save_outputs]{save_outputs()}}
}
