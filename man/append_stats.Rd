% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/append.R
\name{append_stats}
\alias{append_stats}
\title{Append an in-text statistic to an output collection}
\usage{
append_stats(
  output_object,
  object_name = NULL,
  output_list = NULL,
  title = NULL,
  time_stamp = NULL,
  desc = NULL,
  vars = NULL,
  source = NULL,
  notes = NULL,
  text = NULL,
  section = NULL,
  page = NULL,
  type = c("char", "integer", "percent", "float"),
  stat_round = NULL,
  formula = NULL,
  replace = TRUE,
  quiet = NULL
)
}
\arguments{
\item{output_object}{A length-1 numeric or character value.}

\item{object_name}{Name to store and export the plot under. Defaults to
the name of \code{output_object}.}

\item{output_list}{The collection to append to. Defaults to \code{list_outputs}
if visible, otherwise the collection created by the last
\code{\link[=init_outputs]{init_outputs()}}. May be an object, or the name of one.}

\item{title}{Documentation: a human-readable title. Defaults to
\code{object_name}.}

\item{time_stamp}{Documentation: when the output was produced. Defaults to
the current time.}

\item{desc}{Documentation: a free-text description.}

\item{vars}{Documentation: the variables shown, ideally named, e.g.
\code{c(x = "Speed", y = "Braking distance")}.}

\item{source}{Documentation: data sources, ideally named, e.g.
\code{c("CZSO" = "Census 2021")}.}

\item{notes}{Documentation: caveats, to-dos, anything else worth recording.}

\item{text}{Documentation: the sentence the number appears in, with \code{*} as
a placeholder. See \emph{Formula and text templates}.}

\item{section, page}{Documentation: where the statistic is used in the
report.}

\item{type}{How to format the value: \code{"integer"} (thousands separator, no
decimals), \code{"percent"} (multiplied by 100), \code{"float"} (as-is, rounded),
or \code{"char"} (left alone). Default \code{"char"}.}

\item{stat_round}{Number of decimal places for \code{"percent"} and \code{"float"}.
Defaults to \code{gpars$stat_round}.}

\item{formula}{Documentation: how the number was computed. See
\emph{Formula and text templates}.}

\item{replace}{Overwrite an existing entry of the same name. Default \code{TRUE}.}

\item{quiet}{Suppress the confirmation message.}
}
\value{
The collection, invisibly.
}
\description{
Marks a single number (or short string) for export, so that figures quoted
in a report can be traced back to the code that produced them. All
appended statistics are exported to one table, with both the raw value and
a formatted version.
}
\section{Formula and text templates}{

\code{formula} records how the statistic was computed, in terms of the names in
\code{vars}. In the export, a readable version is derived by substituting the
labels: \code{formula = "deaths/population"} with
\code{vars = c(deaths = "Number of deaths", population = "Population")} yields
\code{formula_text = "Number of deaths/Population"}.

\code{text} is the sentence the statistic appears in, with \code{*} marking where the
number goes: \code{text = "Mortality reached * per cent."} produces a
\code{text_fill} column with the formatted value substituted in, ready to paste
into a report.
}

\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
stat_share <- 0.4567
append_stats(stat_share, output_list = out, type = "percent", stat_round = 1,
             formula = "affected/total",
             vars = c(affected = "Affected households",
                      total = "All households"),
             text = "* per cent of households were affected.",
             quiet = TRUE)
names(out$stats)
}
\seealso{
\code{\link[=append_data]{append_data()}}, \code{\link[=save_outputs]{save_outputs()}}
}
