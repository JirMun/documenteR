% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outputs.R
\name{as_outputs}
\alias{as_outputs}
\title{Coerce to an output collection}
\usage{
as_outputs(x, ...)
}
\arguments{
\item{x}{A \code{documenteR_outputs} object (returned unchanged) or a list with
\code{data}, \code{plots}, \code{tables}, \code{stats} and/or \code{gpars} components.}

\item{...}{Unused, for method consistency.}
}
\value{
A \code{documenteR_outputs} object.
}
\description{
Converts a plain nested list — for example a \code{list_outputs.rds} written by
an older version of the package, or by the original standalone script —
into a \code{documenteR_outputs} object. Legacy \code{gpars$ggplot} is mapped to
\code{gpars$sizes}, and any missing general parameters are filled in from
\code{\link[=dr_gpars]{dr_gpars()}}.
}
\examples{
legacy <- list(plots = list(), data = list(), gpars = list(stat_round = 1))
out <- as_outputs(legacy)
out$gpars$stat_round
out$gpars$formats$data # filled in from the defaults
}
