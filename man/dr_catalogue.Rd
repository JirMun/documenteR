% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{dr_catalogue}
\alias{dr_catalogue}
\title{Build a PDF catalogue of plots}
\usage{
dr_catalogue(output_list = NULL, path, project = NULL, items = NULL)
}
\arguments{
\item{output_list}{The collection whose plots to catalogue. See
\code{\link[=append_plot]{append_plot()}}.}

\item{path}{Output PDF path.}

\item{project}{Optional project/version label printed in the footer.}

\item{items}{Optional pre-built item list, used internally by
\code{\link[=save_outputs]{save_outputs()}} so that catalogue entries carry the same numbering as
the exported files. Leave as \code{NULL}.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes one PDF in which every plot gets a page: its documentation typeset
at the top, the plot itself below at its true physical size. Called
automatically by \code{\link[=save_outputs]{save_outputs()}} when \code{catalogue = TRUE}; exported so a
catalogue can be regenerated on its own.
}
\details{
All pages share one size and orientation, and plots keep their aspect
ratio — a plot only ever gets \emph{smaller} than its export size, never
stretched. \code{ggplot2}, \code{lattice} and \code{grid} plots are embedded as vectors;
base-graphics plots are rasterised at \code{gpars$catalogue} resolution,
because base and grid graphics cannot share a page.
}
\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
p1 <- function() plot(cars, pch = 19)
append_plot(p1, output_list = out, desc = "Speed against distance",
            type = "m_wide", quiet = TRUE)
f <- tempfile(fileext = ".pdf")
dr_catalogue(out, f)
file.exists(f)
}
\seealso{
\code{\link[=dr_catalogue_pars]{dr_catalogue_pars()}}, \code{\link[=save_outputs]{save_outputs()}}
}
