% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpars.R
\name{dr_catalogue_pars}
\alias{dr_catalogue_pars}
\title{Appearance of the PDF plot catalogue}
\usage{
dr_catalogue_pars(
  paper = "a4",
  orientation = c("auto", "portrait", "landscape"),
  units = "cm",
  margin = 1.5,
  header_max = 0.32,
  fontsize = 8.5,
  title_fontsize = 12,
  fontfamily = "",
  scale_to_fit = TRUE,
  page_numbers = TRUE,
  include_toc = TRUE
)
}
\arguments{
\item{paper}{Page size: one of \code{"a5"}, \code{"a4"}, \code{"a3"}, \code{"letter"},
\code{"legal"}, or a numeric \code{c(width, height)} in \code{units}.}

\item{orientation}{\code{"auto"} (default) picks portrait or landscape per page
from the plot's aspect ratio; \code{"portrait"} and \code{"landscape"} force one.
Under \code{"auto"} every page still has the \emph{same} dimensions, only rotated,
so PDF viewers no longer rescale each page by a different factor.}

\item{units}{Units for \code{paper} and \code{margin}.}

\item{margin}{Page margin: a single value, or \code{c(top, right, bottom, left)}.}

\item{header_max}{Maximum share of page height (strictly between 0 and 1)
that the documentation header may occupy. The header shrinks to fit its
content but never grows past this, so the plot is never squeezed away.}

\item{fontsize}{Base font size in points for header body text.}

\item{title_fontsize}{Font size in points for the page title.}

\item{fontfamily}{Font family name. \code{""} uses the device default, which is
the safest choice for characters outside Latin-1.}

\item{scale_to_fit}{If \code{TRUE} (default) a plot larger than the available
area is scaled down \emph{proportionally}, preserving its aspect ratio. If
\code{FALSE} it is drawn at full size and clipped.}

\item{page_numbers}{Print "n / N" in the page footer.}

\item{include_toc}{Begin the catalogue with a contents page.}
}
\value{
A named list of catalogue parameters.
}
\description{
The catalogue is a single PDF in which every appended plot gets one page,
drawn at its true physical size with its documentation typeset above it.
This function builds the parameter list controlling its appearance; pass
the result as the \code{catalogue} component of \code{gpars}.
}
\examples{
dr_catalogue_pars(paper = "a3", orientation = "landscape")
}
\seealso{
\code{\link[=dr_gpars]{dr_gpars()}}, \code{\link[=save_outputs]{save_outputs()}}
}
