% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_outputs.R
\name{dr_check_code}
\alias{dr_check_code}
\title{Run analysis scripts in a clean R session}
\usage{
dr_check_code(code_location, quiet = FALSE)
}
\arguments{
\item{code_location}{Paths to \code{.R} files, in run order.}

\item{quiet}{Suppress progress output.}
}
\value{
A list with \code{ok}, \code{total}, \code{failed} and a \code{results} data frame,
invisibly.
}
\description{
Sources each script in a fresh R subprocess, in the order given, and
reports which ones failed. A cheap reproducibility check: it catches the
script that only works because of an object left over in your workspace.
}
\details{
The subprocess has the environment variable \code{DOCUMENTER_RERUN} set to
\code{"1"}. Guard the export at the end of a script with \code{\link[=dr_is_rerun]{dr_is_rerun()}} so the
check does not itself write an export:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{if (!dr_is_rerun()) \{
  save_outputs(output_dir, list_outputs)
\}
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=save_outputs]{save_outputs()}}, \code{\link[=dr_is_rerun]{dr_is_rerun()}}
}
