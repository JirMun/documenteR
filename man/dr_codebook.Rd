% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook.R
\name{dr_codebook}
\alias{dr_codebook}
\title{Describe the variables of a dataset}
\usage{
dr_codebook(data, labels = NULL, max_levels = 5L)
}
\arguments{
\item{data}{A data frame.}

\item{labels}{Optional named character vector of variable labels.}

\item{max_levels}{How many category values to list in the summary before
truncating.}
}
\value{
A data frame with columns \code{variable}, \code{label}, \code{class}, \code{n},
\code{n_missing}, \code{pct_missing}, \code{n_unique} and \code{summary}.
}
\description{
Builds a variable-level codebook: one row per column, with its class, how
much of it is missing, how many distinct values it takes and a compact
summary of its range or most frequent categories.
}
\details{
\code{\link[=save_outputs]{save_outputs()}} writes a codebook covering every appended dataset to
\verb{00_codebook.csv} in the data folder, which is usually the first thing
someone receiving an export wants to read. Set \code{gpars$codebook = FALSE}, or
\code{append_data(codebook = FALSE)} for one dataset, to skip it.

Variable labels are taken from \code{labels}, falling back to a column's
\code{label} attribute — which is what \code{haven} sets when reading SPSS or Stata
files, so labelled survey data documents itself.
}
\examples{
dr_codebook(head(iris), labels = c(Species = "Iris species"))
}
\seealso{
\code{\link[=append_data]{append_data()}}, \code{\link[=save_outputs]{save_outputs()}}
}
