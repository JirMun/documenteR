% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{dr_draw}
\alias{dr_draw}
\alias{dr_draw.default}
\alias{dr_draw.ggplot}
\alias{dr_draw.trellis}
\alias{dr_draw.grob}
\alias{dr_draw.gTree}
\alias{dr_draw.gtable}
\alias{dr_draw.recordedplot}
\alias{dr_draw.function}
\alias{dr_draw.call}
\alias{dr_draw.expression}
\alias{dr_draw.formula}
\title{Draw a plot object on the current graphics device}
\usage{
dr_draw(x, ...)

\method{dr_draw}{default}(x, ...)

\method{dr_draw}{ggplot}(x, ...)

\method{dr_draw}{trellis}(x, ...)

\method{dr_draw}{grob}(x, ...)

\method{dr_draw}{gTree}(x, ...)

\method{dr_draw}{gtable}(x, ...)

\method{dr_draw}{recordedplot}(x, ...)

\method{dr_draw}{`function`}(x, ...)

\method{dr_draw}{call}(x, ...)

\method{dr_draw}{expression}(x, ...)

\method{dr_draw}{formula}(x, ...)
}
\arguments{
\item{x}{The plot object.}

\item{...}{Passed to methods.}
}
\value{
\code{x}, invisibly.
}
\description{
The single point of extension for plot support. \code{documenteR} never calls
\code{ggsave()}; it opens a graphics device itself and asks \code{dr_draw()} to draw
the object on it. That is what makes the package work with any plotting
system, and it is what guarantees that a plot in the PDF catalogue has
exactly the same geometry as the standalone file.
}
\details{
Methods are provided for \code{ggplot2} objects (including \code{patchwork}
compositions), \code{lattice}/\code{trellis} objects, \code{grid} grobs and \code{gtable}s,
recorded base plots, plain drawing functions, and unevaluated calls or
expressions. The default method falls back to \code{print()} and then \code{plot()}.

To support another plotting system, define a method:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{dr_draw.my_plot_class <- function(x, ...) \{
  my_render_function(x)
  invisible(x)
\}
}\if{html}{\out{</div>}}

A method must draw on the \emph{current} device and must not open or close one.
}
\examples{
f <- tempfile(fileext = ".png")
grDevices::png(f, width = 400, height = 300)
dr_draw(function() plot(1:10))
grDevices::dev.off()
file.exists(f)
}
\seealso{
\code{\link[=append_plot]{append_plot()}}, \code{\link[=dr_save_plot]{dr_save_plot()}}
}
