% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writers.R
\name{dr_formats}
\alias{dr_formats}
\title{File formats documenteR can write}
\usage{
dr_formats()
}
\value{
A named list with character vectors for each output type.
}
\description{
Lists the file extensions accepted by \verb{format =} in \code{\link[=append_data]{append_data()}} /
\code{\link[=append_table]{append_table()}} and by \verb{device =} in \code{\link[=append_plot]{append_plot()}}.
}
\details{
Some formats need an optional package, which is only required when you
actually ask for that format:\tabular{ll}{
   Format \tab Package \cr
   \code{.xlsx} \tab \code{openxlsx}, or \code{writexl} \cr
   \code{.parquet} \tab \code{arrow} \cr
   \code{.json} \tab \code{jsonlite} \cr
   \code{.svg} \tab \code{svglite} (falls back to \code{grDevices::svg()}) \cr
   \code{.png}, \code{.jpg}, \code{.tiff} \tab \code{ragg} if installed, otherwise base graphics \cr
}


The PDF catalogue additionally uses \code{png} to place base-graphics plots on
a page alongside their documentation.

\code{.csv} uses \code{gpars$csv_delim} as its separator; \code{.csv2} always uses a
semicolon with a comma decimal mark, which is what Czech and German
installations of Excel expect.
}
\examples{
dr_formats()$data
dr_formats()$plots
}
