% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpars.R
\name{dr_gpars}
\alias{dr_gpars}
\title{Default general parameters for an output collection}
\usage{
dr_gpars(...)
}
\arguments{
\item{...}{Named values to override. Overrides are merged \emph{recursively},
so \code{dr_gpars(formats = list(plots = ".svg"))} changes the plot format
and leaves the other formats at their defaults.}
}
\value{
A named list of general parameters.
}
\description{
Every output collection carries a list of \emph{general parameters} under
\code{gpars}, which supply the defaults used when an individual output does not
specify its own. \code{dr_gpars()} returns that list, so you can inspect the
defaults or build a modified set to hand to \code{\link[=init_outputs]{init_outputs()}}.
}
\details{
The list has the following components.

\describe{
\item{\code{formats}}{Default file formats per output type. See
\code{\link[=dr_formats]{dr_formats()}} for what is supported.}
\item{\code{sizes}}{Plot dimensions. \code{sizes$default} is used when an output
names no \code{type}; \code{sizes$type} holds named presets (\code{"s"}, \code{"m_wide"},
...) that outputs can select with \verb{type =}. Each entry may set
\code{units}, \code{H} (height), \code{W} (width), \code{dpi} and \code{bg} (background).
The defaults are built for an A4 page with 2.5 cm margins, so 16 cm
is the usable text width and 12.35 cm is half the usable height.}
\item{\code{textwidths}}{Character widths used to wrap plot titles, subtitles
and axis labels, per \code{type}. Only applied when
\code{append_plot(wrap_labels = TRUE)}.}
\item{\code{catalogue}}{Appearance of the PDF plot catalogue. See
\code{\link[=dr_catalogue_pars]{dr_catalogue_pars()}}.}
\item{\code{readme_head}}{Title and introductory body text for each output
type's \verb{00_README.txt}.}
\item{\code{readme_w}}{Width in characters to which README text is wrapped.}
\item{\code{encoding}}{Encoding for text output. Only \code{"UTF-8"} is really
supported and it is the default.}
\item{\code{units}}{Default length unit (\code{"cm"}, \code{"mm"}, \code{"in"}).}
\item{\code{stat_round}}{Default number of decimal places for statistics.}
\item{\code{csv_delim}}{Field separator for \code{.csv}. \code{.csv2} always uses
\code{";"} with a comma decimal mark, the convention Czech and German
Excel expects.}
\item{\code{csv_bom}}{Write a UTF-8 byte-order mark at the start of CSV
files. Makes Excel open accented characters correctly, but may
confuse naive parsers. Default \code{FALSE}.}
\item{\code{big_mark}}{Thousands separator used when formatting statistics.
Default \code{" "}, the Czech and SI convention.}
\item{\code{codebook}}{Write a variable-level codebook for appended data.
Default \code{TRUE}.}
\item{\code{hash}}{Record an MD5 checksum of every written file in the
manifest. Default \code{TRUE}.}
}
}
\examples{
gp <- dr_gpars(formats = list(plots = c(".png", ".svg")), stat_round = 2)
gp$formats$plots
gp$formats$data # untouched default
}
\seealso{
\code{\link[=init_outputs]{init_outputs()}}, \code{\link[=dr_catalogue_pars]{dr_catalogue_pars()}}, \code{\link[=dr_formats]{dr_formats()}}
}
