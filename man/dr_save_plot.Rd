% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{dr_save_plot}
\alias{dr_save_plot}
\title{Save a plot to a file at an exact physical size}
\usage{
dr_save_plot(
  plot,
  path,
  W = 16,
  H = 12.35,
  units = "cm",
  dpi = 300,
  bg = "white",
  family = ""
)
}
\arguments{
\item{plot}{A plot object — anything \code{\link[=dr_draw]{dr_draw()}} handles.}

\item{path}{Output file path. Its extension selects the format.}

\item{W, H}{Width and height.}

\item{units}{Units for \code{W} and \code{H}: \code{"cm"}, \code{"mm"}, \code{"in"} or \code{"px"}.}

\item{dpi}{Resolution for raster formats, or \code{"retina"}/\code{"print"}/\code{"screen"}.}

\item{bg}{Background colour.}

\item{family}{Font family; \code{""} uses the device default.}
}
\value{
\code{path}, invisibly.
}
\description{
Opens a graphics device of exactly \code{W} x \code{H} \code{units}, draws \code{plot} on it
with \code{\link[=dr_draw]{dr_draw()}} and closes it. This is the low-level workhorse behind
\code{\link[=save_outputs]{save_outputs()}}; it is exported because it is genuinely useful on its own,
and because it is the function to test against when a plot does not come
out the size you expect.
}
\examples{
p <- function() barplot(c(a = 3, b = 5, c = 2))
f <- tempfile(fileext = ".png")
dr_save_plot(p, f, W = 12, H = 8, units = "cm")
file.exists(f)
}
\seealso{
\code{\link[=dr_draw]{dr_draw()}}, \code{\link[=preview_plot]{preview_plot()}}
}
