% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version.R
\name{dr_version_name}
\alias{dr_version_name}
\title{Generate a memorable name for a version}
\usage{
dr_version_name(used = character(0), parts = .dr_name_parts, max_tries = 200L)
}
\arguments{
\item{used}{Character vector of names already taken.}

\item{parts}{A list of word vectors with components \code{whose}, \code{who},
\code{named}, \code{how} and \code{what}. Defaults to the built-in Czech word lists;
supply your own to change the flavour.}

\item{max_tries}{How many times to retry before giving up on uniqueness.}
}
\value{
A single string. Falls back to a timestamp-based name if the
vocabulary is exhausted, so this function never loops indefinitely.
}
\description{
Builds a short whimsical sentence used as a version's human-readable name,
avoiding names already used in the same project.
}
\examples{
set.seed(1)
dr_version_name()
}
\seealso{
\code{\link[=save_outputs]{save_outputs()}}
}
