% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{folder_setup}
\alias{folder_setup}
\title{Set up the folder structure for an export}
\usage{
folder_setup(output_list = NULL, output_dir = NULL, folders = dr_folders())
}
\arguments{
\item{output_list}{The collection. See \code{\link[=append_plot]{append_plot()}}.}

\item{output_dir}{The version folder to create the structure inside.}

\item{folders}{Named character vector mapping output type to folder name.
Must contain \code{output} plus any of \code{data}, \code{plots}, \code{tables}, \code{stats}.}
}
\value{
A named character vector of created folder paths.
}
\description{
Creates the per-type subfolders inside a version folder and returns their
paths. Only folders for output types that actually contain something are
created, so an export with no tables has no empty tables folder.
}
\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
d <- cars
append_data(d, output_list = out, quiet = TRUE)
dirs <- folder_setup(out, file.path(tempdir(), "demo_export"))
basename(dirs)
}
\seealso{
\code{\link[=save_outputs]{save_outputs()}}
}
