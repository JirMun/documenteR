% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readme.R
\name{get_output_README}
\alias{get_output_README}
\title{Build the README text for one output type}
\usage{
get_output_README(output_list = NULL, output_type, plan = NULL)
}
\arguments{
\item{output_list}{The collection. See \code{\link[=append_plot]{append_plot()}}.}

\item{output_type}{One of \code{"output"}, \code{"data"}, \code{"plots"}, \code{"tables"},
\code{"stats"}.}

\item{plan}{Optional export plan, used internally so that README headings
match the numbered file names exactly. Leave as \code{NULL}.}
}
\value{
A single string containing the README text.
}
\description{
Renders the documentation of every appended output of one type into the
plain-text README written alongside the files. Called by \code{\link[=save_outputs]{save_outputs()}};
exported so you can preview what an export will document before running it.
}
\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
d <- cars
append_data(d, output_list = out, desc = "Speed and stopping distance",
            source = c(datasets = "R built-in"), quiet = TRUE)
cat(get_output_README(out, "data"))
}
\seealso{
\code{\link[=save_outputs]{save_outputs()}}
}
