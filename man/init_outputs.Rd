% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outputs.R
\name{init_outputs}
\alias{init_outputs}
\title{Initialise an output collection}
\usage{
init_outputs(default = TRUE, gpars = dr_gpars(), quiet = NULL)
}
\arguments{
\item{default}{If \code{TRUE} (default), assign the new collection to
\code{list_outputs} in the calling environment \emph{and} register it as the
active collection, so \verb{append_*()} and \code{\link[=save_outputs]{save_outputs()}} find it without
being told. If \code{FALSE}, only return it.}

\item{gpars}{General parameters. Defaults to \code{\link[=dr_gpars]{dr_gpars()}}; pass
\code{dr_gpars(...)} to override individual settings.}

\item{quiet}{Suppress the confirmation message.}
}
\value{
A \code{documenteR_outputs} object, invisibly when \code{default = TRUE}.
}
\description{
Creates the collection that \code{\link[=append_plot]{append_plot()}}, \code{\link[=append_data]{append_data()}},
\code{\link[=append_table]{append_table()}} and \code{\link[=append_stats]{append_stats()}} add to, and that \code{\link[=save_outputs]{save_outputs()}}
exports. Call it once near the top of an analysis script.
}
\details{
The collection is an environment with class \code{documenteR_outputs}. Because
environments have reference semantics, \verb{append_*()} adds to it \emph{in place}
— you never have to write \code{list_outputs <- append_plot(list_outputs, p)}.
It otherwise behaves like the nested list it replaces:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{length(out$plots)                    # number of appended plots
out$gpars$formats$plots <- ".svg"    # change a general parameter
names(out$data)                      # names of appended datasets
}\if{html}{\out{</div>}}

With \code{default = TRUE} the collection is additionally assigned to
\code{list_outputs} in the calling environment, which for a script run at top
level is the global environment. That is what allows the rest of the
\verb{append_*()} calls to omit \code{output_list}.
}
\examples{
# A self-contained collection, not registered anywhere
out <- init_outputs(default = FALSE, quiet = TRUE)
out

# Custom defaults
out2 <- init_outputs(
  default = FALSE,
  gpars = dr_gpars(formats = list(plots = c(".png", ".pdf"))),
  quiet = TRUE
)
out2$gpars$formats$plots
}
\seealso{
\code{\link[=dr_gpars]{dr_gpars()}}, \code{\link[=append_plot]{append_plot()}}, \code{\link[=save_outputs]{save_outputs()}}
}
