% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{preview_plot}
\alias{preview_plot}
\title{Preview a plot at its export size}
\usage{
preview_plot(
  plot,
  output_list = NULL,
  type = NULL,
  H = NULL,
  W = NULL,
  units = NULL,
  dpi = NULL,
  device = NULL,
  bg = NULL,
  open = TRUE,
  quiet = NULL
)
}
\arguments{
\item{plot}{A plot object, or the name of an already-appended plot.}

\item{output_list}{The collection whose \code{gpars} supply the defaults. See
\code{\link[=append_plot]{append_plot()}}.}

\item{type, H, W, units, dpi, device, bg}{Geometry overrides; anything omitted
comes from \code{type}, then from \code{gpars$sizes$default}.}

\item{open}{Open the file in the system viewer. Default \code{TRUE}.}

\item{quiet}{Suppress the confirmation message.}
}
\value{
The path to the temporary file, invisibly.
}
\description{
Saves a plot to a temporary file using exactly the geometry
\code{\link[=save_outputs]{save_outputs()}} would use, and opens it in the system viewer. The quickest
way to check whether a chosen \code{type} or \code{H}/\code{W} actually suits a plot,
before committing to a full export.
}
\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
p <- function() plot(cars)
f <- preview_plot(p, output_list = out, type = "s_wide", open = FALSE)
basename(f)
}
\seealso{
\code{\link[=append_plot]{append_plot()}}, \code{\link[=dr_save_plot]{dr_save_plot()}}
}
