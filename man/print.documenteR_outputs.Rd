% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{print.documenteR_outputs}
\alias{print.documenteR_outputs}
\title{Print an output collection}
\usage{
\method{print}{documenteR_outputs}(x, n = 8L, detail = FALSE, ...)
}
\arguments{
\item{x}{A \code{documenteR_outputs} object.}

\item{n}{Maximum number of outputs to list per type. Use \code{Inf}, or
\code{detail = TRUE}, to list all of them.}

\item{detail}{List every output and show the description column.}

\item{...}{Unused.}
}
\value{
\code{x}, invisibly.
}
\description{
Shows what a collection currently holds: how many outputs of each type,
the file name each will be exported under, its title, and how completely
it is documented. The listing uses the same numbering \code{\link[=save_outputs]{save_outputs()}}
will use, so it doubles as a preview of the export.
}
\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
data_cars <- cars
append_data(data_cars, output_list = out, desc = "Built-in data",
            quiet = TRUE)
plt_cars <- function() plot(cars)
append_plot(plt_cars, output_list = out, type = "s_wide", quiet = TRUE)

out
print(out, detail = TRUE)
}
\seealso{
\code{\link[=summary.documenteR_outputs]{summary.documenteR_outputs()}} for the full inventory as a data
frame, \code{\link[=init_outputs]{init_outputs()}}, \code{\link[=save_outputs]{save_outputs()}}
}
