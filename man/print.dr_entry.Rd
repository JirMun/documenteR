% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{print.dr_entry}
\alias{print.dr_entry}
\title{Print one appended output}
\usage{
\method{print}{dr_entry}(x, ...)
}
\arguments{
\item{x}{A \code{dr_entry} object — one element of \code{x$plots}, \code{x$data},
\code{x$tables} or \code{x$stats}.}

\item{...}{Unused.}
}
\value{
\code{x}, invisibly.
}
\description{
Renders the documentation of a single appended output, so that inspecting
one shows what you recorded about it rather than dumping the plot object
or the whole data frame:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list_outputs$plots$plt_cars
}\if{html}{\out{</div>}}
}
\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
plt_cars <- function() plot(cars)
append_plot(plt_cars, output_list = out, type = "m_wide",
            desc = "Speed against braking distance",
            vars = c(x = "Speed", y = "Distance"),
            source = c(datasets = "R built-in"), quiet = TRUE)

out$plots$plt_cars
}
\seealso{
\code{\link[=append_plot]{append_plot()}}, \code{\link[=print.documenteR_outputs]{print.documenteR_outputs()}}
}
