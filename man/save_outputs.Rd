% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_outputs.R
\name{save_outputs}
\alias{save_outputs}
\title{Export everything in an output collection}
\usage{
save_outputs(
  output_dir,
  output_list = NULL,
  project_title = "",
  version_notes = "",
  new_version = FALSE,
  update_latest_version = FALSE,
  version_name = NULL,
  exclude = character(0),
  code_location = character(0),
  catalogue = TRUE,
  rerun_code = FALSE,
  folders = dr_folders(),
  dry_run = FALSE,
  quiet = NULL
)
}
\arguments{
\item{output_dir}{Project output folder. A version subfolder is created
inside it.}

\item{output_list}{The collection to export. Defaults to \code{list_outputs} if
visible, otherwise the collection created by the last \code{\link[=init_outputs]{init_outputs()}}.}

\item{project_title}{Project title recorded in \verb{00_DOC.txt}. If empty, the
title of the previous version is carried forward.}

\item{version_notes}{What changed in this version. Worth filling in.}

\item{new_version}{If \code{TRUE}, bump the major version (\verb{0_3} becomes
\verb{1_0}); otherwise bump the minor version (\verb{0_3} becomes \verb{0_4}).}

\item{update_latest_version}{If \code{TRUE}, overwrite the most recent version
folder instead of creating a new one. For fixing a typo, not for new
results.}

\item{version_name}{Override the generated version name.}

\item{exclude}{Output types to leave out, e.g. \code{c("data")}. Their folder
still gets a README recording that they were excluded, so the omission is
documented rather than invisible.}

\item{code_location}{Paths to script files to copy into the export. Give
every script the analysis needs, in run order.}

\item{catalogue}{Build the PDF plot catalogue. See \code{\link[=dr_catalogue_pars]{dr_catalogue_pars()}}.}

\item{rerun_code}{If \code{TRUE}, run \code{code_location} in a clean R subprocess
first and refuse to export if it fails. See \code{\link[=dr_check_code]{dr_check_code()}}.}

\item{folders}{Folder names per output type. See \code{\link[=dr_folders]{dr_folders()}}.}

\item{dry_run}{If \code{TRUE}, report exactly what would be written and write
nothing.}

\item{quiet}{Suppress progress output. The log is still written.}
}
\value{
A \code{documenteR_export} object (invisibly): a list with the version,
the path written to, the manifest and the log.
}
\description{
Writes a complete, self-describing export of a collection into a new
version folder: the files themselves, a README per output type, a
machine-readable manifest with checksums, a variable codebook for the
data, a typeset PDF catalogue of the plots, a copy of the code, and a log
of everything that happened along the way.
}
\section{What gets written}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{<output_dir>/0_4/
  00_DOC.txt         version number, name, notes, session info
  00_LOG.txt         every message and warning raised during the export
  00_MANIFEST.csv    one row per file: object, documentation, size, MD5
  00_MANIFEST.json   the same, plus export metadata
  01_output/         the collection object and a copy of the code
  02_data/           datasets + 00_codebook.csv + 00_README.txt
  03_plots/          plots + 00_01_catalogue.pdf + 00_README.txt
  04_tables/         tables + 00_README.txt
  05_stats/          01_stats.csv + 00_README.txt
}\if{html}{\out{</div>}}

Folders are only created for output types that contain something.
}

\section{Console output and the log}{

Messages and warnings raised by other packages during an export are
captured into \verb{00_LOG.txt} rather than printed, so the progress report
stays readable. Warnings are then summarised at the end — nothing is
silently dropped. A single output that cannot be written is recorded as
\code{failed} in the manifest and the export continues, so one broken plot does
not cost you the other forty files.
}

\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
data_cars <- cars
append_data(data_cars, output_list = out, desc = "Built-in cars data",
            quiet = TRUE)
plt_cars <- function() plot(cars, pch = 19)
append_plot(plt_cars, output_list = out, desc = "Speed vs distance",
            quiet = TRUE)

dir <- file.path(tempdir(), "documenteR_example")
res <- save_outputs(dir, out, project_title = "Example", quiet = TRUE)
res$version
list.files(res$path)
}
\seealso{
\code{\link[=init_outputs]{init_outputs()}}, \code{\link[=append_plot]{append_plot()}}, \code{\link[=dr_versions]{dr_versions()}},
\code{\link[=dr_manifest]{dr_manifest()}}, \code{\link[=dr_verify]{dr_verify()}}, \code{\link[=dr_log]{dr_log()}}
}
