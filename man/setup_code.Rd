% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_code.R
\name{setup_code}
\alias{setup_code}
\title{Create a starter analysis script}
\usage{
setup_code(
  script_file = "01_01_data",
  code_folder = "04_code",
  project_title = "Project",
  output_dir = NULL,
  output_kind = "01_data",
  packages_file = NULL,
  custom_funs = NULL,
  template = NULL,
  overwrite = FALSE,
  open = TRUE,
  quiet = NULL,
  wd = getwd()
)
}
\arguments{
\item{script_file}{Name of the script to create, without the \code{.R}
extension.}

\item{code_folder}{Folder to create it in, relative to \code{wd}.}

\item{project_title}{Project title written into the \code{save_outputs()} call.}

\item{output_dir}{Output folder expression written into the script. The
default builds a path from \code{output_kind} and the script name.}

\item{output_kind}{Sub-folder of the analysis output tree, e.g.
\code{"01_data"}.}

\item{packages_file}{Optional script to \code{source()} for library calls.}

\item{custom_funs}{Optional script of project-specific functions to
\code{source()}.}

\item{template}{Path to a template file. Defaults to the one shipped with
the package; \code{documenteR:::.dr_template_path()} shows where that is.}

\item{overwrite}{Overwrite an existing file. When \code{FALSE} (default) a
\verb{_new} suffix is used instead.}

\item{open}{Open the new file in RStudio, if available.}

\item{quiet}{Suppress the confirmation message.}

\item{wd}{Project root.}
}
\value{
The path to the script written, invisibly.
}
\description{
Writes a script pre-wired for the \code{documenteR} workflow: it initialises a
collection, leaves a body section for the analysis, and ends with a
\code{\link[=save_outputs]{save_outputs()}} call guarded by \code{\link[=dr_is_rerun]{dr_is_rerun()}}.
}
\examples{
d <- file.path(tempdir(), "proj")
dir.create(file.path(d, "04_code"), recursive = TRUE, showWarnings = FALSE)
f <- setup_code("01_01_data", wd = d, open = FALSE)
basename(f)
}
\seealso{
\code{\link[=init_outputs]{init_outputs()}}, \code{\link[=save_outputs]{save_outputs()}}
}
