% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{summary.documenteR_outputs}
\alias{summary.documenteR_outputs}
\alias{print.documenteR_summary}
\title{The full inventory of an output collection}
\usage{
\method{summary}{documenteR_outputs}(object, ...)

\method{print}{documenteR_summary}(x, ...)
}
\arguments{
\item{object}{A \code{documenteR_outputs} object.}

\item{...}{Unused.}

\item{x}{A \code{documenteR_summary} object.}
}
\value{
A data frame with class \code{documenteR_summary}, carrying columns
\code{type}, \code{id}, \code{name}, \code{title}, \code{file}, \code{formats}, \code{size}, \code{desc},
\code{source}, \code{subfolder}, \code{has_desc}, \code{has_vars}, \code{has_source} and
\code{doc_fields}. It prints as an aligned table and subsets like any data
frame.
}
\description{
One row per appended output, with the file it will be exported to, its
documentation and how completely it is documented. Unlike
\code{\link[=print.documenteR_outputs]{print.documenteR_outputs()}} nothing is truncated, so this is what to use
to find the outputs that still need attention:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{s <- summary(list_outputs)
s[!s$has_desc, c("type", "name")]
}\if{html}{\out{</div>}}
}
\examples{
out <- init_outputs(default = FALSE, quiet = TRUE)
d <- cars
append_data(d, output_list = out, desc = "Documented", quiet = TRUE)
t1 <- head(cars)
append_table(t1, output_list = out, quiet = TRUE)

s <- summary(out)
s
# which outputs still need a description?
s$name[!s$has_desc]
}
\seealso{
\code{\link[=print.documenteR_outputs]{print.documenteR_outputs()}}, \code{\link[=save_outputs]{save_outputs()}}
}
